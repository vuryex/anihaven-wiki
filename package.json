{
  "name": "anihaven-wiki",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run generate-dates && next dev",
    "build": "npm run generate-dates && next build",
    "export": "npm run build && next export",
    "start": "next start",
    "lint": "next lint",
    "generate-dates": "node scripts/generate-dates.js",
    "generate-dates:force": "rm -f lib/file-dates.json && node scripts/generate-dates.js",
    "clean": "rm -rf .next out lib/file-dates.json",
    "fresh-build": "npm run clean && npm run generate-dates && next build"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "clsx": "^2.1.1",
    "github-slugger": "^2.0.0",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0",
    "marked": "^9.1.0",
    "next": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "swr": "^2.2.0",
    "vercel": "^44.2.11"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.0",
    "@types/node": "^20.19.4",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "autoprefixer": "^10.4.16",
    "eslint": "^8.0.0",
    "eslint-config-next": "14.0.0",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.3.5",
    "typescript": "^5.0.0"
  }
}
